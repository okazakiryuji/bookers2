<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h1>New book</h1>

      <%= form_with model: @User, url: '/users', method: :pos do |f| %>
         <h4>Title</h4>
         <%= f.text_field :title %>

         <h4>Opinion</h4>
         <%= f.text_area :body %>

       <%= f.submit 'Create Book' %>
      <% end %>
    </div>
  </div>
</div>

